<div id="resultStr">

    {{if searchStr.length > 4}}
        По вашему запросу "${searchStr}" найдено ${our_data.length} документов
    {{else}}
        Количество символов при поиске должно быть больше 4-рех
    {{/if}}

</div>

<div class="clear"></div>

<div id="resultWrap"></div>
